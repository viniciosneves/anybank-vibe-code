# Anybank

Anybank é um banco 100% online, focado em agilidade e praticidade.

## Backend

Vamos utilizar java e springboot (API Rest + swagger + postgres (use docker compose com um volume configurado para levantar o banco) )

## Frontend

Vamos utilizar Angular com:

- atomic design pros componentes
- tailwind
